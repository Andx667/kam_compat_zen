# common

Shared functions used by other addons in this mod. Currently empty scaffolding (depends on `cba_main`, `ace_main`, `kcz_main`, `zen_main`) — kept as a place for cross-component helpers so individual bridge addons don't duplicate logic as more KAM components get covered.
