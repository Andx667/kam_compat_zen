# data

Drop the in-game icon here as `logo_ca.paa` — referenced by `CfgMods` in
[`../config.cpp`](../config.cpp) via `\z\kcz\addons\main\data\logo_ca.paa`.

See [`workshop/badge-concept.svg`](../../../workshop/badge-concept.svg) for
the source concept and conversion steps.
